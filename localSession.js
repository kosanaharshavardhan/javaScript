//local storage
// 1. .setItems(key,value)
// note it only takes  strngs use json
//.getItems(key)
// .remove(items)
// .clear();
// local upto 5mb stays in browser until unser explicitly deletes it
// session stays until browser window is open